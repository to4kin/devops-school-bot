name: Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    container: golang:1.17
    
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2

      - name: Build
        run: make all
